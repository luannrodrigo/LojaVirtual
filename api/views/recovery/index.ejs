<!doctype html>
<html lang="pt-br">
<head>
    <% include ../partials/header.ejs %>
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col s4 m7">
                <h2>Recuperar senha</h2>
                <hr/><br>

                <% if(success){ %>
                    <div class="chip">
                        <%= success %>
                    </div>
                <%}%>
                <% if (error) {%>
                    <div class="chip">
                        <%= error%>
                    </div>
                <%}%>
                <p>Digite seu e-mail para receber as instruções de como reiniciar a sua senha</p>
                <form action="/v1/api/usuarios/recuperar-senha" method="post">
                    <div class="row">
                        <div class="col s12">
                            <input id="email" name="email" type="email" class="validate" placeholder="Digite o seu e-mail">
                            <label for="email">Email</label>
                        </div>
                    </div>
                    <button class="waves-effect waves-light btn col s12">Recuperar senha</button>
                </form>
            </div>
        </div>
    </div>
    <!-- Compiled and minified JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>